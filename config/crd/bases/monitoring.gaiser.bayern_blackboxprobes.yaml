---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: blackboxprobes.monitoring.gaiser.bayern
spec:
  group: monitoring.gaiser.bayern
  names:
    kind: BlackboxProbe
    listKind: BlackboxProbeList
    plural: blackboxprobes
    singular: blackboxprobe
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.exporterRef.name
      name: Exporter
      type: string
    - jsonPath: .spec.moduleRef.name
      name: Module
      type: string
    - jsonPath: .status.targetCount
      name: Targets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlackboxProbe is the Schema for the blackboxprobes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BlackboxProbeSpec defines the desired state of BlackboxProbe.
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: additionalLabels are added to the generated Probe CR
                  and propagate to scraped metrics.
                type: object
              exporterRef:
                description: exporterRef references the BlackboxExporter instance
                  to use.
                properties:
                  name:
                    description: name of the referenced resource.
                    type: string
                  namespace:
                    description: |-
                      namespace of the referenced resource.
                      Defaults to the namespace of the referencing resource.
                    type: string
                required:
                - name
                type: object
              ingress:
                description: |-
                  ingress configures target discovery from Ingress resources.
                  The operator configures a target for each host of matching Ingress objects.
                  At least one of targets or ingress must be set.
                properties:
                  namespaceSelector:
                    description: namespaceSelector selects namespaces to discover
                      Ingress objects from.
                    properties:
                      any:
                        description: any selects all namespaces.
                        type: boolean
                      matchNames:
                        description: matchNames is a list of namespace names to select.
                        items:
                          type: string
                        type: array
                    type: object
                  relabelConfigs:
                    description: |-
                      relabelConfigs are applied to the discovered Ingress targets before scraping.
                      Available labels: __tmp_prometheus_ingress_address, __tmp_prometheus_job_name.
                    items:
                      description: RelabelConfig defines metric relabeling configuration.
                      properties:
                        action:
                          default: replace
                          description: action is the relabeling action to perform.
                          enum:
                          - replace
                          - keep
                          - drop
                          - hashmod
                          - labelmap
                          - labeldrop
                          - labelkeep
                          type: string
                        modulus:
                          description: modulus is used with hashmod action.
                          format: int64
                          type: integer
                        regex:
                          default: (.*)
                          description: regex is the regular expression to match against.
                          type: string
                        replacement:
                          default: $1
                          description: replacement is the replacement value for regex
                            matches.
                          type: string
                        separator:
                          default: ;
                          description: separator is the separator used to concatenate
                            source labels.
                          type: string
                        sourceLabels:
                          description: sourceLabels is a list of label names to use
                            as input.
                          items:
                            type: string
                          type: array
                        targetLabel:
                          description: targetLabel is the label to write the result
                            to.
                          type: string
                      type: object
                    type: array
                  selector:
                    description: selector selects Ingress objects by label.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              interval:
                default: 60s
                description: interval is the scrape interval for the generated Probe
                  CR.
                type: string
              metricRelabelings:
                description: metricRelabelings are applied to the generated Probe
                  CR.
                items:
                  description: RelabelConfig defines metric relabeling configuration.
                  properties:
                    action:
                      default: replace
                      description: action is the relabeling action to perform.
                      enum:
                      - replace
                      - keep
                      - drop
                      - hashmod
                      - labelmap
                      - labeldrop
                      - labelkeep
                      type: string
                    modulus:
                      description: modulus is used with hashmod action.
                      format: int64
                      type: integer
                    regex:
                      default: (.*)
                      description: regex is the regular expression to match against.
                      type: string
                    replacement:
                      default: $1
                      description: replacement is the replacement value for regex
                        matches.
                      type: string
                    separator:
                      default: ;
                      description: separator is the separator used to concatenate
                        source labels.
                      type: string
                    sourceLabels:
                      description: sourceLabels is a list of label names to use as
                        input.
                      items:
                        type: string
                      type: array
                    targetLabel:
                      description: targetLabel is the label to write the result to.
                      type: string
                  type: object
                type: array
              moduleRef:
                description: moduleRef references the BlackboxModule to use for probing.
                properties:
                  name:
                    description: name of the referenced resource.
                    type: string
                  namespace:
                    description: |-
                      namespace of the referenced resource.
                      Defaults to the namespace of the referencing resource.
                    type: string
                required:
                - name
                type: object
              scrapeTimeout:
                default: 10s
                description: scrapeTimeout is the scrape timeout. Must be <= interval.
                type: string
              targets:
                description: |-
                  targets is a list of static endpoints to probe (URLs or host:port).
                  At least one of targets or ingress must be set.
                items:
                  type: string
                type: array
            required:
            - exporterRef
            - moduleRef
            type: object
          status:
            description: BlackboxProbeStatus defines the observed state of BlackboxProbe.
            properties:
              conditions:
                description: conditions represent the current state of the BlackboxProbe
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last observed .metadata.generation.
                format: int64
                type: integer
              probeRef:
                description: probeRef is a reference to the generated prometheus-operator
                  Probe CR.
                properties:
                  name:
                    description: name of the referenced resource.
                    type: string
                  namespace:
                    description: |-
                      namespace of the referenced resource.
                      Defaults to the namespace of the referencing resource.
                    type: string
                required:
                - name
                type: object
              targetCount:
                description: targetCount is the number of configured targets.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
